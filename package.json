{
  "name": "codenames-game",
  "version": "0.0.1",
  "description": "Codenames online game implementation",
  "author": "koldoon",
  "private": true,
  "license": "GPL 3.0",
  "scripts": {
    "install:server": "cd ./server && npm i --no-fund",
    "install:frontend": "cd ./frontend && npm i --no-fund",
    "install": "npx run-s install:*",
    "build:server": "cd ./server && npm run build",
    "build:frontend": "cd ./frontend && npm run build",
    "build": "npx run-s build:*",
    "start": "cd ./server && npm start"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
